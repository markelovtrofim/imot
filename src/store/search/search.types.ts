export type RequestDataType = {
  key: string,
  values: string[]
};

export type CriteriasType = {
  title: string,
  key: string,
  selectType: string,
  addMulti: boolean,
  values: string[],
  block: string,
  wide: boolean,
};